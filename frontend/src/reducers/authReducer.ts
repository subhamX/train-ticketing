

export default (state = [], action:any) => {
    switch(action.type){
        case 'auth/login':{
            return state;
        }
        default:{
            return state;
        }
    }
}